import { FC } from "react";
import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Colleges = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>This is colllges</Text>
      </View>
    </SafeAreaView>
  );
};

export default Colleges;
