import { FC } from "react";
import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Rankings = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>This is rankings</Text>
      </View>
    </SafeAreaView>
  );
};

export default Rankings;
